import React from 'react';

//import './settings.scss';

const Settings = () => {
    return(
        <div className="section settings">
            Settings
        </div>
    );
}

export default Settings;


// TODO:
// 1. check if 'settings' class is needed here: className="section settings"
// 2. add proptypes